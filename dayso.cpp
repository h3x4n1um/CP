#include <bits/stdc++.h>

using namespace std;

int curr, prev, n;

int main(){
    freopen("dayso.inp", "r", stdin);
    freopen("dayso.out", "w", stdout);
    scanf("%d%d%d", &prev, &curr, &n);
    return 0;
}
