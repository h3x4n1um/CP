#include <bits/stdc++.h>

using namespace std;

long long temp;

int main(){
    while (scanf("%lld", &temp) != EOF){
        if (temp == 19){
            printf("%lld", temp);
            return 0;
        }
    }
    printf("IMPOSSIBLE");
    return 0;
}
