#include <bits/stdc++.h>

using namespace std;

int n;

int main(){
    scanf("%d", &n);
    for (int i = 0; i < n; ++i){
        int x, y, z;
        scanf("%d%d%d", &x, &y, &z);
    }
    return 0;
}
