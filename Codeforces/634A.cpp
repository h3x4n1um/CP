#include <bits/stdc++.h>

using namespace std;

int n;
vector <int> a[2];

int main(){
    scanf("%d", &n);
    for (int i = 0; i < n; ++i){
        int temp;
        scanf("%d", &temp);
        a[0].push_back(temp);
    }
    for (int i = 0; i < n; ++i){
        int temp;
        scanf("%d", &temp);
        a[1].push_back(temp);
    }
    for (int i = 0; i < n; ++i){

    }
    return 0;
}
