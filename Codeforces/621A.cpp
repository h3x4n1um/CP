#include <bits/stdc++.h>

using namespace std;

vector <int> a;

int main(){
    int n;
    scanf("%d", &n);
    for (int i = 0; i < n; ++i){
        int temp;
        a.push_back(temp);
    }
    return 0;
}
