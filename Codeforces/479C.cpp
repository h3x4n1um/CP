#include <bits/stdc++.h>

using namespace std;

int n;
vector <int> a, b;

int main(){
    scanf("%d", &n);
    for (int i = 0; i < n; ++i){
        scanf("%d%d", &a[i], &b[i]);
    }

    return 0;
}
