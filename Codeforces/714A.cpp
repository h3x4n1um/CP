#include <bits/stdc++.h>

int l1, l2, r1, r2, k;

int main(){
    scanf("%d%d%d%d%d", &l1, &r1, &l2, &r2, &k);
    if (l1 <= l2 && r1 >= l2){
        if (r2 <= r1){
            if (k >= l2 && k <= r2) printf("%d", );
        }
        else{
            if ()
        }
    }
    else printf("%d", 0);
    return 0;
}
